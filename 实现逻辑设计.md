# 微流控AI助手 — 实现逻辑设计文档

> 面向开发者的技术设计文档，详述系统架构、通信协议、核心模块与数据流。

---

## 1. 技术选型与理由

### 1.1 后端技术栈

| 技术 | 版本 | 选型理由 |
|------|------|---------|
| Flask | 2.0.1 | 轻量级 Web 框架，适合中小型 API 服务，蓝图机制便于模块化拆分 |
| OpenAI SDK | >= 2.32.0 | DeepSeek 使用 OpenAI 兼容接口，SDK 原生支持流式和工具调用 |
| PyYAML | 6.0.1 | 配置文件管理，支持复杂嵌套结构，可读性优于 JSON |
| Flask-Cors | 3.0.10 | 跨域支持，开发/生产环境统一处理 CORS |
| requests | - | 千问 DashScope API 的 HTTP 通信，支持重试和流式响应 |

### 1.2 前端技术栈

| 技术 | 版本 | 选型理由 |
|------|------|---------|
| Vue 3 | 3.4.21 | 组合式 API（Composition API）+ `<script setup>` 语法，逻辑复用优于选项式 |
| TypeScript | 5.3+ | 类型安全，IDE 智能提示，降低运行时错误 |
| Vite | 5.1.6 | 基于 ESM 的快速构建和 HMR，开发体验远优于 Webpack |
| Pinia | 2.1.7 | 新一代 Vue 状态管理，TypeScript 原生支持，API 简洁 |
| Element Plus | 2.5.6 | 企业级 UI 组件库，与 Vue 3 深度集成 |
| Marked | - | Markdown 解析，将 AI 输出转为 HTML |
| Mermaid | - | 流程图/时序图渲染，可视化液滴路径规划 |
| Highlight.js | - | 代码语法高亮，Mermaid 降级时展示源码 |

---

## 2. 系统架构设计

### 2.1 后端分层架构

```
表现层（Controllers）→ 业务逻辑层（Services）→ 数据访问层（Config/Data）→ 基础设施层（Utils）
```

**控制器层（5 个蓝图）：**

| 蓝图 | URL 前缀 | 职责 |
|------|---------|------|
| health_bp | `/api` | 健康检查 |
| api_mgmt_bp | `/api` | API 密钥管理、模型列表、配置 CRUD |
| stream_bp | `/api` | SSE 流式通信（两步式 + POST 直连） |
| prompt_bp | `/api` | 提示词模板管理 |
| chip_layout_bp | `/api` | 芯片网格布局 CRUD |

**服务层（7 个服务）：**

| 服务 | 职责 |
|------|------|
| ConfigService | 加载/缓存 YAML 配置（api.yaml, settings.yaml） |
| ApiService | 统一 API 调用入口，速率限制，提示词模板合并，Agent Loop |
| ApiClient（工厂模式） | 抽象基类 + OpenAIClient / QwenClient 实现 |
| PromptService | 版本化提示词模板加载与管理 |
| ChipLayoutService | 芯片网格数据的加载、验证、持久化、格式化输出 |
| DropletToolService | 液滴分配串口协议封装（Mock/真实双模式） |
| ToolRegistry | 工具注册表，管理工具定义与执行分发 |

**工具层：**

| 模块 | 职责 |
|------|------|
| config_utils.py | ConfigLoader，带缓存的 YAML 文件加载器 |
| api_utils.py | ChatCompletionRequest / ChatCompletionResponse 数据模型，请求/响应解析 |
| common_utils.py | 通用工具函数：消息构建、速率限制检查、配置验证、请求头构建 |

### 2.2 前端组件化架构

```
App.vue (根组件)
  └─ router-view
      ├─ ChatRoom.vue (对话页面)
      │   ├─ MessageBubble.vue (消息气泡，约996行)
      │   │   └─ ContentParser.ts    — Markdown/Mermaid 内容分段解析
      │   │   └─ MermaidRenderer.ts  — Mermaid 图表渲染与降级
      │   │   └─ SvgExporter.ts      — SVG 导出功能
      │   ├─ ChatInput.vue (输入框，支持 Shift+Enter 换行)
      │   └─ ChipPreviewPanel.vue (悬浮预览面板)
      │       └─ ChipGridMini.vue (网格缩略图)
      ├─ ApiConfig.vue (API 配置页面)
      └─ GridConfig.vue (网格配置页面)
```

---

## 3. SSE 两步式通信协议

### 3.1 协议流程

```
前端                                后端
  │                                  │
  │─── POST /api/stream/init ───────→│  发送对话参数
  │←── { stream_id } ───────────────│  返回唯一标识
  │                                  │
  │─── GET /api/stream/{stream_id} ─→│  建立 EventSource 连接
  │←── data: {...}\n\n ─────────────│  逐块推送 SSE 数据
  │←── data: {...}\n\n ─────────────│
  │←── data: [DONE]\n\n ───────────│  结束标记
  │                                  │
```

**为什么用两步式：** 浏览器原生 `EventSource` 仅支持 GET 请求，无法携带 POST Body。两步式方案先通过 POST 上传参数并获取 `stream_id`，再通过 GET 建立 SSE 连接。参数存储在服务端内存字典 `_stream_requests` 中，TTL 为 60 秒，一次性消费（pop）。

### 3.2 SSE 响应头配置

定义在 `backend/controllers/base.py`：

```python
SSE_HEADERS = {
    'Content-Type': 'text/event-stream',
    'Cache-Control': 'no-cache, no-store, must-revalidate',
    'X-Accel-Buffering': 'no',
    'Connection': 'keep-alive',
    'Access-Control-Allow-Origin': '*',
    'Access-Control-Allow-Methods': 'GET, OPTIONS',
    'Access-Control-Allow-Headers': 'Content-Type, Authorization',
}
```

关键点：
- `X-Accel-Buffering: no` — 禁止 Nginx 缓冲 SSE 流
- `Cache-Control: no-cache, no-store, must-revalidate` — 禁止任何层级缓存
- `Connection: keep-alive` — 保持长连接

### 3.3 事件数据格式

```
# 正文内容增量
data: {"choices": [{"delta": {"content": "你好"}}]}

# 思维链增量（DeepSeek 深度思考模式）
data: {"choices": [{"delta": {"reasoning_content": "分析中..."}}]}

# 工具执行状态通知
data: {"type": "tool_status", "message": "正在执行: dispense_droplet..."}

# 工具执行结果
data: {"type": "tool_result", "tool_name": "dispense_droplet", "result": "..."}

# 流结束标记
data: [DONE]
```

### 3.4 Vite 代理配置

在 `frontend/vite.config.ts` 中，针对 SSE 路由特殊处理：

```typescript
proxy: {
  '/api': {
    target: 'http://localhost:5000',
    changeOrigin: true,
    configure: (proxy, options) => {
      proxy.on('proxyReq', (proxyReq, req, res) => {
        if (req.url?.includes('/stream')) {
          proxyReq.setHeader('Cache-Control', 'no-cache');
        }
      });
      proxy.on('proxyRes', (proxyRes, req, res) => {
        if (req.url?.includes('/stream')) {
          proxyRes.headers['x-accel-buffering'] = 'no';
          proxyRes.headers['cache-control'] = 'no-cache';
        }
      });
    }
  }
}
```

---

## 4. Agent Loop 工具调用机制

### 4.1 流程概述（最多 5 轮迭代）

```
┌─────────────────────────────────────────────────────────┐
│  1. 构建请求（包含 tools 定义，tool_choice='auto'）       │
│  2. 流式调用 API，实时推送文本和思维链到前端               │
│  3. 同时累积 tool_calls 增量（_accumulate_tool_calls）   │
│  4. 流结束后检查 accumulated_tool_calls                  │
│     ├─ 有工具调用：                                      │
│     │   a. 推送 tool_status 事件                         │
│     │   b. 执行工具（tool_registry.execute_tool）         │
│     │   c. 推送 tool_result 事件                         │
│     │   d. 工具结果追加到 messages 历史                    │
│     │   e. 继续下一轮迭代                                 │
│     └─ 无工具调用：                                      │
│         推送 [DONE]，结束流                               │
└─────────────────────────────────────────────────────────┘
```

核心实现位于 `ApiService.agentic_stream_api()` 方法。

### 4.2 工具定义（dispense_droplet）

```json
{
  "type": "function",
  "function": {
    "name": "dispense_droplet",
    "description": "控制微流控芯片执行液滴分配操作",
    "parameters": {
      "type": "object",
      "properties": {
        "electrode_sequences": {
          "type": "array",
          "description": "多个液滴的电极序列，每个元素为一个液滴的完整路径"
        },
        "interval": {
          "type": "number",
          "default": 1.0,
          "description": "每个时间步之间的间隔秒数"
        }
      },
      "required": ["electrode_sequences"]
    }
  }
}
```

### 4.3 API 适配策略

| API 类型 | api_type | 工具调用支持 | 行为 |
|---------|----------|-------------|------|
| DeepSeek | `openai` | 完整支持 | 使用 `agentic_stream_api()`，流式 + 工具调用循环 |
| 千问 | `qwen` | 不支持 | 自动降级为 `stream_api()`，普通流式对话，忽略 tools_enabled |

降级判断逻辑位于 `stream_controller.py` 的 `stream_api_response()` 函数中。

### 4.4 tool_calls 增量累积

流式响应中 `tool_calls` 以增量方式到达，需要通过 `_accumulate_tool_calls()` 函数按 `index` 拼接：

```python
def _accumulate_tool_calls(accumulated: dict, delta_tool_calls: list):
    for tc in delta_tool_calls:
        idx = tc.get('index', 0)
        if idx not in accumulated:
            accumulated[idx] = {
                'id': tc.get('id', ''),
                'type': 'function',
                'function': {'name': tc.get('function', {}).get('name', ''), 'arguments': ''}
            }
        else:
            if tc.get('id'):
                accumulated[idx]['id'] = tc['id']
            if tc.get('function', {}).get('name'):
                accumulated[idx]['function']['name'] = tc['function']['name']
        if tc.get('function', {}).get('arguments'):
            accumulated[idx]['function']['arguments'] += tc['function']['arguments']
```

---

## 5. 模块设计详解

### 5.1 API 客户端工厂模式

```
BaseApiClient (抽象基类，定义接口)
  ├─ chat_completions()           — 非流式调用
  ├─ stream_chat_completions()    — 流式调用（Generator）
  ├─ chat_completions_with_tools() — 非流式 + 保留原始 tool_calls
  └─ get_models()                 — 获取模型列表
      │
      ├─ OpenAIClient (DeepSeek, 所有 OpenAI 兼容服务)
      │   └─ 使用 openai.OpenAI SDK，延迟初始化（@property client）
      │   └─ 支持 thinking_enabled / reasoning_effort（DeepSeek 深度思考）
      │   └─ 支持 tools / tool_choice
      │
      └─ QwenClient (阿里云千问 DashScope)
          └─ 使用 requests.Session + HTTPAdapter 重试策略
          └─ 请求适配：统一格式 → 千问格式（build_qwen_request）
          └─ 响应适配：千问累积文本 → OpenAI 增量格式（delta 计算）
          └─ SSE 头：X-DashScope-SSE: enable

ApiClientFactory.create_client(api_name, config) → BaseApiClient
  └─ 内部注册表：{'openai': OpenAIClient, 'qwen': QwenClient}
  └─ 按 config['api_type'] 选择实现类
```

**客户端缓存：** `ApiService._get_api_client()` 将已创建的客户端缓存在 `self.clients` 字典中，避免重复创建。更新 API Key 时自动清除对应缓存。

### 5.2 提示词模板系统

**版本化目录结构：**
```
backend/config/prompts/
  └─ v1/
      ├─ prompt_config.yaml      — 当前使用的配置
      └─ prompt_config_v1.yaml   — 备份
```

**模板占位符：**
- `{chip_layout}` — 当前芯片布局信息（由 ChipLayoutService.format_for_prompt() 生成）
- `{user_requirements}` — 用户原始输入

**渲染流程：**
```
PromptService.get_prompt_config(version)
  → 加载 YAML 模板文件
  → ApiService._merge_prompt_with_template(user_input, prompt_config)
    → ChipLayoutService.format_for_prompt() 获取芯片布局字符串
    → template.format(chip_layout=..., user_requirements=...)
    → 返回合并后的完整提示词
```

**芯片布局自动注入：** 无论是 `stream_api()` 还是 `agentic_stream_api()`，都会在调用前自动加载 v1 提示词模板，将当前芯片布局信息和用户输入合并后发送给 LLM。

### 5.3 芯片布局管理

**数据结构：**
- 17×22 二维整数数组
- `0` 表示禁区（不可激活）
- `1-128` 为电极编号

**验证规则：**
- 行数 = 17
- 列数 = 22
- 值范围 0–128
- 由 ChipLayoutService 在保存时校验

**持久化：**
- 主存储：`data/chip_layout.json`（JSON 文件）
- 备选：`config/settings.yaml` 中的默认配置
- 优先级：自定义 JSON 文件 > YAML 默认配置

---

## 6. Mermaid 渲染降级策略

### 6.1 四层降级机制

实现位于 `frontend/src/components/message/MermaidRenderer.ts`：

```
第1层：sanitizeMermaidCode() 净化代码后渲染
  ↓ 失败
第2层：若净化修改了代码，用原始代码重试
  ↓ 失败
第3层：使用 highlight.js 语法高亮显示 Mermaid 源码
  ↓ 失败
第4层：最终降级为 HTML 转义纯文本（<pre> 标签）
```

### 6.2 流式渲染优化

| 优化策略 | 实现细节 |
|---------|---------|
| 互斥锁 | 防止并发更新导致渲染不稳定，同一时刻只有一个渲染任务执行 |
| 防抖 | 流式阶段 80ms 防抖，平衡实时性和性能 |
| Hash 缓存 | 已渲染的 Mermaid 块通过内容 hash 去重，流式→完成阶段继承已渲染结果，避免重复渲染 |
| 超时控制 | 单个图表渲染 10 秒超时，超时后自动降级 |

---

## 7. 状态管理系统（Pinia）

### 7.1 chatStore（chat.ts）

| 分类 | 内容 |
|------|------|
| State | `conversations: Conversation[]`, `currentConversationId: string \| null`, `isLoading`, `isStreaming`, `error`, `eventSource: EventSource \| null` |
| Computed | `currentConversation`, `currentMessages` |
| Actions | `sendMessage(content)` — SSE 全流程（init → EventSource → 事件处理）<br>`createConversation()` — 创建新对话<br>`stopGeneration()` — 关闭 EventSource 中断生成<br>`loadConversations()` / `saveConversations()` — localStorage 持久化 |
| 持久化键 | `microfluidic_conversations` |

**sendMessage 流程：**
1. 添加用户消息到当前对话
2. POST `/api/stream/init` 获取 `stream_id`
3. 创建 `EventSource` 连接到 `/api/stream/{stream_id}`
4. 监听 `message` 事件，逐事件处理：
   - `reasoning_content` → 追加到 `message.reasoningContent`
   - `tool_status` → 添加到 `toolCalls` 数组（status: executing）
   - `tool_result` → 更新对应 toolCall 的 status 和 result
   - `delta.content` → 追加到 `message.content`
   - `[DONE]` → 设置 `isStreaming=false`，关闭连接
5. 保存到 localStorage

### 7.2 apiConfigStore（apiConfig.ts）

| 分类 | 内容 |
|------|------|
| State | `currentApi: 'qwen' \| 'deepseek'`, `apiKeys: {qwen, deepseek}`, `modelConfig`（含 `thinkingEnabled`, `reasoningEffort`） |
| Actions | `saveConfig()` — 同步到后端 + localStorage<br>`loadConfig()` — 从 localStorage 恢复<br>`validateConfig()` — 验证 API 密钥有效性 |
| 持久化键 | `microfluidic_api_config` |

### 7.3 chipLayoutStore（chipLayout.ts）

| 分类 | 内容 |
|------|------|
| State | `grid: number[][]`, `statistics`（电极数量等统计信息）, `floatingPanel`（visible/minimized/position/opacity） |
| Actions | `fetchLayout(force?)` — 从后端获取布局（30s 缓存）<br>`notifyLayoutUpdated()` — 通过 BroadcastChannel 通知其他标签页 |
| 持久化键 | `chip-preview-panel-state`（仅面板 UI 状态） |

---

## 8. 数据持久化策略

| 数据 | 存储方式 | 键值 / 路径 |
|------|---------|------------|
| 对话列表 | localStorage | `microfluidic_conversations` |
| API 配置 | localStorage + 后端 API | `microfluidic_api_config` |
| 悬浮面板状态 | localStorage | `chip-preview-panel-state` |
| 芯片网格数据 | 后端 JSON 文件 | `data/chip_layout.json` |
| API 密钥 | 后端 YAML 文件 | `config/api.yaml` |
| 提示词模板 | 后端 YAML 文件 | `config/prompts/v1/prompt_config.yaml` |

---

## 9. 跨标签页通信

- **技术：** BroadcastChannel API（频道名：`chip-layout-sync`）
- **场景：** GridConfig 页面提交网格配置后，通知其他标签页的 ChipPreviewPanel 刷新显示

**流程：**
```
GridConfig 提交成功
  → chipLayoutStore.notifyLayoutUpdated()
    → BroadcastChannel.postMessage({ type: 'layout-updated' })
      → 其他标签页监听 message 事件
        → chipLayoutStore.fetchLayout(force=true) 强制刷新
```

---

## 10. 前后端数据流全景

```
用户输入 → ChatInput @send → ChatRoom handleSendMessage
  → chatStore.sendMessage(content)
    → POST /api/stream/init
       携带: api_name, model, prompt, tools_enabled,
             thinking_enabled, reasoning_effort, messages(历史)
    → 返回 stream_id
    → new EventSource(/api/stream/{stream_id})
      → 后端 stream_controller.stream_by_id()
        → 从 _stream_requests 取出并消费参数
        → stream_api_response()
          ├─ tools_enabled=true & api_type='openai':
          │   → ApiService.agentic_stream_api()
          │     → 加载 v1 提示词模板
          │     → 合并芯片布局信息
          │     → Agent Loop（最多5轮）:
          │       → BaseApiClient.stream_chat_completions()
          │       → 实时推送文本/思维链
          │       → 累积 tool_calls
          │       → 执行工具 → 推送 tool_status/tool_result
          │       → 继续或结束
          ├─ tools_enabled=true & api_type='qwen':
          │   → 降级为 ApiService.stream_api()（普通流式）
          └─ tools_enabled=false:
              → ApiService.stream_api()
                → 加载提示词模板 → 合并芯片布局
                → BaseApiClient.stream_chat_completions()
                → SSE 逐块推送
      → 前端逐事件处理：
        - reasoning_content → message.reasoningContent 追加
        - tool_status       → toolCalls.push({status:'executing'})
        - tool_result       → toolCall.status='completed', toolCall.result=...
        - delta.content     → message.content 追加
        - [DONE]            → isStreaming=false, 关闭 EventSource
    → MessageBubble 响应式更新
      → ContentParser.parseContent() 分段
      → MermaidRenderer.renderMermaidSegments() 渲染图表
    → saveConversations() 持久化到 localStorage
```

---

## 11. 液滴工具串口协议

### 11.1 帧格式（29 字节）

```
偏移    字节数    含义                          值/说明
─────────────────────────────────────────────────────────
[0-1]     2     帧头                           0xDD 0xD1
[2]       1     长度                           0x18 (固定24字节)
[3]       1     OutputType 标志                 0x01=ElectrodeOnly, 0x02=PowerOnly, 0x08=ElectrodeAndPower
[4-19]   16     电极位图                        128个电极的开/关状态，每bit对应一个电极
[20]      1     电压码                          反向线性映射: code = round((103-V)/(103-46)*255)
[21]      1     AC 开关                         0x01=开, 0x00=关
[22-23]   2     频率                           大端序 (high byte first)
[24-26]   3     保留字节                        填充 0x00
[27]      1     CRC                            (长度 + sum(Data区所有字节)) & 0xFF
[28]      1     帧尾                           0x0A
```

**电极位图编码规则：**
```python
# 电极编号 n (1-128) → 位图位置
grp = (n - 1) // 8       # 字节组索引 0-15
bit = 7 - ((n - 1) % 8)  # 位索引 7-0（高位优先）
data[1 + grp] |= (1 << bit)
```

### 11.2 运行模式

| 模式 | 配置 | 行为 |
|------|------|------|
| Mock 模式（开发） | `mock_mode: true` | 仅生成帧数据和日志，不打开串口，通过 `time.sleep(interval)` 模拟时间步间隔 |
| 真实模式（生产） | `mock_mode: false` | 通过 pyserial 打开串口（默认 COM6, 115200），写入帧数据并 flush |

### 11.3 执行流程

```
ToolRegistry.execute_tool('dispense_droplet', arguments)
  → DropletToolService.execute_dispense(electrode_sequences, interval, voltage, output_type)
    → 遍历时间步 t = 0..max_steps:
      → 合并所有液滴在时间步 t 的电极列表
      → build_frame(nodes, voltage, output_type) 构造 29 字节帧
      → Mock: 记录日志 / 真实: ser.write(frame) + ser.flush()
      → time.sleep(interval)
    → 返回 {success, total_steps, executed_steps, log_messages}
```
