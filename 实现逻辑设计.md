# 微流控AI助手 - 实现逻辑设计文档

## 1. 技术架构设计

### 1.1 前端技术栈

| 技术组件 | 选型 | 版本 | 用途 |
|---------|------|------|------|
| 框架 | Vue 3 | 3.4+ | 核心前端框架 |
| 构建工具 | Vite | 5.0+ | 快速构建和热更新 |
| 语言 | TypeScript | 5.3+ | 类型安全开发 |
| 状态管理 | Pinia | 2.1+ | 全局状态管理 |
| 路由管理 | Vue Router | 4.2+ | 页面路由控制 |
| UI组件库 | Element Plus | 2.4+ | UI组件库 |
| HTTP客户端 | Axios | 1.6+ | API请求库 |
| CSS预处理器 | SCSS | - | 样式预处理 |

### 1.2 后端技术栈

| 技术组件 | 现有技术 | 版本 | 用途 |
|---------|---------|------|------|
| Web框架 | Flask | 2.0.1 | 核心后端框架 |
| 实时通信 | SSE (Server-Sent Events) | - | 实时数据推送 |
| 配置管理 | PyYAML | 6.0.1 | YAML配置文件解析 |
| CORS | Flask-Cors | 3.0.10 | 跨域支持 |

### 1.3 前后端通信规范

#### RESTful API接口规范
- **数据格式**：JSON
- **请求编码**：UTF-8
- **响应格式统一**：
  ```json
  {
    "code": 200,
    "message": "ok",
    "data": {}
  }
  ```

#### 实时数据传输
- **技术选型**：SSE (Server-Sent Events)
- **用途**：对话实时输出、状态更新
- **数据格式**：
  ```
  data: {"id":"xxx","choices":[{"delta":{"content":"..."}}]}\n\n
  data: [DONE]\n\n
  ```

---

## 2. 页面功能模块划分

### 2.1 模型API配置页面

#### 页面布局
```
┌─────────────────────────────────────────┐
│  顶部导航栏                                │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │  API配置表单                        │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ API选择 (千问/DeepSeek)     │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ API密钥输入                  │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 模型参数设置                  │  │  │
│  │  │ - 模型选择                   │  │  │
│  │  │ - 最大token数                │  │  │
│  │  │ - 温度参数                   │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ [测试连接] [保存配置]        │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

#### 配置项详情

| 配置项 | 类型 | 必填 | 验证规则 | 默认值 |
|-------|------|------|---------|--------|
| API类型 | 单选 | 是 | qwen/deepseek | qwen |
| API密钥 | 字符串 | 是 | 长度≥10，非占位符 | - |
| 模型名称 | 下拉选择 | 是 | 从API获取 | - |
| 最大Token数 | 数字 | 否 | 1-32768 | 1024 |
| 温度参数 | 数字 | 否 | 0-2 | 0.7 |
| Top P | 数字 | 否 | 0-1 | 1.0 |

#### 保存机制
- 前端保存到localStorage + Pinia状态
- 调用后端API `/api/api/key` 更新配置
- 自动验证配置有效性

---

### 2.2 对话与实时输出页面

#### 页面布局（参考豆包设计）
```
┌────────────────────────────────────────────────────────┐
│  顶部导航栏                                             │
├────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────┐  │
│  │  对话历史侧边栏          │      对话主区域       │  │
│  │  ┌──────────────────┐    │  ┌────────────────┐  │  │
│  │  │ 历史对话1        │    │  │ [消息气泡]      │  │  │
│  │  │ 历史对话2        │    │  │ [消息气泡]      │  │  │
│  │  │ 历史对话3        │    │  │ [实时输出...]   │  │  │
│  │  │ + 新建对话       │    │  └────────────────┘  │  │
│  │  └──────────────────┘    │  ┌────────────────┐  │  │
│  │                          │  │ 输入框 [发送]   │  │  │
│  │                          │  └────────────────┘  │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

#### 核心功能模块

**1. 消息气泡**
- 用户消息：右侧对齐，浅蓝背景
- AI消息：左侧对齐，白色背景，带头像
- 支持Markdown渲染
- 支持代码高亮
- 复制功能

**2. 输入区域**
- 多行文本输入框
- 支持Shift+Enter换行
- Enter发送消息
- 最大字符数限制（4096字）
- 清空按钮

**3. 交互控制**
- 停止生成按钮（生成中显示）
- 清空对话按钮
- 复制当前对话按钮
- 重新生成按钮

---

## 3. 数据流程设计

### 3.1 API配置数据流程

```
┌─────────────┐
│ 用户输入配置  │
└──────┬──────┘
       │
       ▼
┌──────────────────┐
│ 前端表单验证      │
└──────┬───────────┘
       │
       ▼
┌──────────────────────────────┐
│ Pinia状态更新 + localStorage │
└──────┬───────────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 调用后端API /api/api/key │
└──────┬───────────────────┘
       │
       ▼
┌────────────────────┐
│ 后端更新配置文件    │
└────────────────────┘
```

### 3.2 对话数据流程

```
┌──────────────────┐
│ 用户输入消息      │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ 前端状态更新      │
│ 显示用户消息      │
└──────┬───────────┘
       │
       ▼
┌─────────────────────────────┐
│ 发送POST请求 /api/api/stream │
└──────┬──────────────────────┘
       │
       ▼
┌─────────────────────────┐
│ 建立SSE连接             │
│ 接收实时数据流          │
└──────┬──────────────────┘
       │
       ▼
┌──────────────────────────┐
│ 前端逐步渲染消息内容      │
│ 更新对话历史              │
└──────────────────────────┘
```

### 3.3 实时输出数据流处理逻辑

```javascript
// SSE连接处理流程
1. 创建EventSource或使用fetch实现SSE
2. 监听message事件
3. 解析JSON数据
4. 提取delta.content
5. 追加到当前AI消息
6. 更新UI（自动滚动到底部）
7. 收到[DONE]标记后关闭连接
8. 保存完整对话到历史
```

---

## 4. 状态管理设计

### 4.1 Pinia Store 结构

#### 4.1.1 API配置Store (`useApiConfigStore`)

```typescript
interface ApiConfigState {
  currentApi: 'qwen' | 'deepseek'
  apiKeys: {
    qwen: string
    deepseek: string
  }
  modelConfig: {
    model: string
    maxTokens: number
    temperature: number
    topP: number
  }
  isConfigValid: boolean
}

interface ApiConfigActions {
  setApiType(type: 'qwen' | 'deepseek'): void
  setApiKey(api: string, key: string): void
  setModelConfig(config: Partial<ModelConfig>): void
  validateConfig(): Promise<boolean>
  saveConfig(): Promise<void>
  loadConfig(): void
}
```

#### 4.1.2 对话Store (`useChatStore`)

```typescript
interface Message {
  id: string
  role: 'user' | 'assistant'
  content: string
  timestamp: number
  isStreaming?: boolean
}

interface Conversation {
  id: string
  title: string
  messages: Message[]
  createdAt: number
  updatedAt: number
}

interface ChatState {
  conversations: Conversation[]
  currentConversationId: string | null
  isLoading: boolean
  isStreaming: boolean
  error: string | null
}

interface ChatActions {
  createConversation(): string
  selectConversation(id: string): void
  deleteConversation(id: string): void
  sendMessage(content: string): Promise<void>
  stopGeneration(): void
  clearCurrentConversation(): void
  appendStreamingContent(content: string): void
}
```

### 4.2 状态更新与组件通信

```
组件事件触发
    ↓
调用Store Action
    ↓
更新State
    ↓
响应式更新UI
    ↓
（可选）持久化到localStorage
```

---

## 5. 错误处理与边界情况

### 5.1 API配置错误处理

| 错误类型 | 处理策略 | 用户提示 |
|---------|---------|---------|
| API密钥为空 | 表单验证阻止提交 | "请输入API密钥" |
| API密钥无效 | 调用验证接口 | "API密钥无效，请检查" |
| 网络连接失败 | 重试机制 + 提示 | "网络连接失败，请检查网络" |
| 模型不支持 | 模型列表过滤 | "模型不存在，请重新选择" |

### 5.2 对话过程中的异常处理

| 异常场景 | 处理机制 |
|---------|---------|
| API调用超时 | 显示超时提示，允许重试 |
| 速率限制 | 显示倒计时，自动重试 |
| 内容生成中断 | 保留已生成内容，提供继续生成选项 |
| 服务端错误 | 友好错误提示，记录日志 |

### 5.3 网络连接中断应对方案

```
1. 检测连接状态（navigator.onLine）
2. 离线时缓存用户输入
3. 连接恢复后自动重试
4. 显示连接状态指示器
5. 提供手动重试按钮
```

---

## 6. 性能优化考虑

### 6.1 前端渲染优化

| 优化策略 | 实现方式 |
|---------|---------|
| 虚拟滚动 | 对话历史过多时使用 |
| 懒加载 | 历史对话列表分页加载 |
| 消息分片渲染 | 长消息分片显示 |
| 防抖节流 | 输入框输入、搜索等 |
| 代码分割 | 路由级别的代码分割 |

### 6.2 数据缓存机制

| 缓存内容 | 存储位置 | 过期策略 |
|---------|---------|---------|
| API配置 | localStorage | 永久（手动更新） |
| 对话历史 | IndexedDB | 永久（可手动删除） |
| 模型列表 | Pinia + localStorage | 24小时 |
| 常用提示词 | localStorage | 永久 |

### 6.3 资源加载优化

- 使用Vite的自动预加载
- 图片资源懒加载
- CDN加速第三方库
- Gzip压缩
- 按需引入Element Plus组件

---

## 7. 前后端API接口补充

### 7.1 新增API接口

| 接口 | 方法 | 功能 |
|-----|------|------|
| `/api/chat/sessions` | GET | 获取对话历史列表 |
| `/api/chat/sessions` | POST | 创建新对话 |
| `/api/chat/sessions/:id` | GET | 获取指定对话详情 |
| `/api/chat/sessions/:id` | DELETE | 删除对话 |
| `/api/chat/sessions/:id/messages` | POST | 添加消息到对话 |

---

## 8. 项目目录结构规划

```
微流控/
├── backend/              # 现有Flask后端
│   ├── app.py
│   ├── controllers/
│   ├── services/
│   ├── utils/
│   ├── config/
│   ├── requirements.txt
│   └── README.md
│
└── frontend/             # 新建Vue3前端
    ├── public/
    ├── src/
    │   ├── assets/          # 静态资源
    │   ├── components/      # 公共组件
    │   │   ├── MessageBubble.vue
    │   │   ├── ChatInput.vue
    │   │   └── ApiConfigForm.vue
    │   ├── views/           # 页面组件
    │   │   ├── ApiConfig.vue
    │   │   └── ChatRoom.vue
    │   ├── router/          # 路由配置
    │   │   └── index.ts
    │   ├── stores/          # Pinia状态管理
    │   │   ├── apiConfig.ts
    │   │   └── chat.ts
    │   ├── api/             # API接口
    │   │   └── index.ts
    │   ├── utils/           # 工具函数
    │   ├── types/           # TypeScript类型
    │   ├── App.vue
    │   └── main.ts
    ├── package.json
    ├── vite.config.ts
    ├── tsconfig.json
    └── README.md
```

---

## 9. 开发计划

### Phase 1: 项目初始化
- 创建Vue3 + Vite项目
- 配置TypeScript、Pinia、Vue Router
- 配置Element Plus
- 搭建基础项目结构

### Phase 2: API配置页面
- 实现配置表单
- 实现表单验证
- 对接后端API
- 配置本地存储

### Phase 3: 对话页面
- 实现消息气泡组件
- 实现输入框组件
- 实现SSE实时通信
- 实现对话历史管理

### Phase 4: 优化与测试
- 性能优化
- 错误处理完善
- 跨浏览器测试
- 编写文档

---

*本设计文档待审核确认后，将开始具体代码开发实施。*
